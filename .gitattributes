# Normalize all text files to use LF line endings
* text=auto

# PHP files should use LF line endings
*.php text eol=lf

# Shell scripts should use LF line endings
*.sh text eol=lf

# YAML files should use LF line endings
*.yml text eol=lf
*.yaml text eol=lf

# Markdown files should use LF line endings
*.md text eol=lf

# CSS and JS files should use LF line endings
*.css text eol=lf
*.js text eol=lf

# Batch files should use CRLF line endings
*.bat text eol=crlf
